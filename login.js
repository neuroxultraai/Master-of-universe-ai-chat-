import { store } from '../app.js'; export function Login(){ const wrap=document.createElement('div'); wrap.className='card'; const email=document.createElement('input'); email.className='input'; email.placeholder='Email'; const btn=document.createElement('button'); btn.className='button'; btn.textContent='Login (demo)'; btn.onclick=()=>{ if(!email.value) return alert('Email required'); store.set('user',{email:email.value}); localStorage.setItem('mou_token','demo-token'); location.hash='#dashboard'; }; wrap.appendChild(email); wrap.appendChild(btn); return wrap;}